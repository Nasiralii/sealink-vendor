import { CommonModule } from "@angular/common";
import { FormsModule, ReactiveFormsModule } from "@angular/forms";
import { RouterLink, RouterModule } from "@angular/router";


export const vendorSharedModules = [
    CommonModule,
    RouterLink,
    RouterModule,
    ReactiveFormsModule,
    FormsModule
] 